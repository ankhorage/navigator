---
'@ankhorage/navigator': patch
---

Keep standalone example applications and generated documentation synchronized with each published
Navigator version, and restore example acceptance in repository-owned CI.
